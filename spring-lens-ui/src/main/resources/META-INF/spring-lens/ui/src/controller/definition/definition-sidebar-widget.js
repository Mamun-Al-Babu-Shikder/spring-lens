import { BeanSidebarWidget } from '../shared/bean-sidebar-widget.js';

export default class DefinitionSidebarWidget extends BeanSidebarWidget {
    constructor(options = {}) {
        super(options);
    }
}
